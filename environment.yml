name: optica_env
runs-on: ubuntu-latest
channels:
    - conda-forge
dependencies:
    - python
    - pip
    - setuptools
    - numpy
    - matplotlib
    - jupyterlab=2.2.10
    - ipywidgets=7.6.5
    - ipympl
