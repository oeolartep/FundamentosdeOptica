name: optica_env
runs-on: ubuntu-latest
channels:
    - conda-forge
dependencies:
    - python
    - pip
    - setuptools
    - numpy
    - matplotlib
    - jupyterlab
    - ipywidgets=7.6.5
    - ipympl
